<div class="row">
  <div class="col s12">
    <div class="row">
      <div class="col s12">
        <br>
        <a class="btn-floating btn-large waves-effect waves-light" href="<%= new_note_path %>">
          <i class="material-icons">add</i>
        </a>
        <span>Create a note</span>
      </div>
    </div>

    <% if @notes.empty? %>
      <p>There is not notes.</p>
    <% else %>
      <% @notes.each do |note| %>
        <div class="row">
          <div class="col s12">
            <div class="card blue-grey lighten-4">
              <div class="card-content">
                <span class="card-title"><%= note.title.html_safe %></span>
                <p><%= note.content.html_safe %></p>
              </div>

              <div class="card-action">
                <%= link_to(note, class: 'btn-floating red', method: :delete) do %>
                  <i class="material-icons">delete</i>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
